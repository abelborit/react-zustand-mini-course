/* configuración básica para usar el cliente de React Query */
import { QueryClient } from "@tanstack/react-query";

/* instanciar la clase QueryClient */
const queryClient = new QueryClient();

export default queryClient;
